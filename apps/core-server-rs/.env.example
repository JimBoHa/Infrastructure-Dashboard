# Rust core-server example environment file
# Copy to `.env` and adjust as needed for local development.

# Logging
# RUST_LOG=info

# Core runtime
CORE_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/iot
CORE_MQTT_HOST=127.0.0.1
CORE_MQTT_PORT=1883
# CORE_MQTT_USERNAME=
# CORE_MQTT_PASSWORD=

# Dashboard static assets (built output directory)
# If unset, the server still runs but will not serve the dashboard UI.
CORE_STATIC_ROOT=/usr/local/farm-dashboard/static/dashboard-web

# Setup daemon proxy used by the Setup Center UI (optional)
CORE_SETUP_DAEMON_BASE_URL=http://127.0.0.1:8800

# Backups
CORE_BACKUP_STORAGE_PATH=/Users/Shared/FarmDashboard/storage/backups
CORE_BACKUP_RETENTION_DAYS=30

# Node agent discovery/health (HTTP, optional)
CORE_NODE_AGENT_PORT=9000

# Optional external feeds (fixtures/http providers handled by the controller stack)
CORE_ENABLE_ANALYTICS_FEEDS=false
CORE_ENABLE_FORECAST_INGESTION=false
CORE_SCHEDULE_POLL_INTERVAL_SECONDS=15
# CORE_FORECAST_API_BASE_URL=http://127.0.0.1:9103
# CORE_FORECAST_API_PATH=/forecast.json
# CORE_ANALYTICS_RATES__API_BASE_URL=http://127.0.0.1:9104
# CORE_ANALYTICS_RATES__API_PATH=/rates.json

# Predictive alarms config is stored in the DB and managed via `/api/predictive/config`.
# Defaults to disabled on fresh installs.
