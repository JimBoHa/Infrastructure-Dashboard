/**
 * NodeButton — thin re-export of the shared Button component.
 * Kept so existing imports from features/nodes/components/NodeButton continue to work.
 */
export { Button as default } from "@/components/ui/button";
