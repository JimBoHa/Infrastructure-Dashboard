/**
 * NodePill — thin re-export of the shared Badge component.
 * Kept so existing imports from features/nodes/components/NodePill continue to work.
 */
export { Badge as default, type BadgeTone as NodePillTone } from "@/components/ui/badge";
