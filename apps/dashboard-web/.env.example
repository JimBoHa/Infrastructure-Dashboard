# Dashboard Web example environment file
#
# Next.js reads `.env.local` (preferred for local dev), so copy this file:
#   cp .env.example .env.local

FARM_CORE_API_BASE=http://127.0.0.1:8000
FARM_SETUP_DAEMON_BASE=http://127.0.0.1:8800
# Legacy fallback for rewrites (avoid in production bundles):
# NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
# When core-server auth is enabled, supply a bearer token for mutations.
# NEXT_PUBLIC_AUTH_TOKEN=

# Dev-only helper: show a "Login as Dev" button on /login (localhost only).
# NEXT_PUBLIC_ENABLE_DEV_LOGIN=1
# NEXT_PUBLIC_DEV_LOGIN_EMAIL=playwright@farmdashboard.local
# NEXT_PUBLIC_DEV_LOGIN_PASSWORD=CHANGE_ME

# Screenshot harness can also auto-login if these are set:
# FARM_SCREENSHOT_LOGIN_EMAIL=playwright@farmdashboard.local
# FARM_SCREENSHOT_LOGIN_PASSWORD=CHANGE_ME
# FARM_SCREENSHOT_ALLOW_STUB_FALLBACK=1
