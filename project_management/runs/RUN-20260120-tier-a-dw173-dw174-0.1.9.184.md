# RUN-20260120 — Tier A — DW-173/DW-174 Analytics Overview audit + Display order modal scroll (0.1.9.184)

- **Date:** 2026-01-20
- **Tier:** A (installed controller refresh; **no DB/settings reset**)
- **Controller version:** 0.1.9.184
- **Controller bundle DMG:** `/Users/Shared/FarmDashboardBuilds/FarmDashboardController-0.1.9.184.dmg`

## Scope

- DW-173: Analytics Overview UI/layout/rendering audit fixes:
  - Water usage 24h chart: prevent mixed-horizon series from producing overlapping/garbled x-axis ticks.
  - Weather forecast + PV forecast vs measured: normalize forecast timestamps and ensure charts render reliably (no “blank chart” confusion).
- DW-174: Display order modal: ensure viewport-safe scrolling and reachable content.

## Build controller bundle DMG

```bash
cargo run --manifest-path apps/farmctl/Cargo.toml -- bundle \
  --version 0.1.9.184 \
  --output /Users/Shared/FarmDashboardBuilds/FarmDashboardController-0.1.9.184.dmg \
  --native-deps /usr/local/farm-dashboard/native
```

Result: DMG created.

## Refresh installed controller (Upgrade)

```bash
curl -fsS -X POST http://127.0.0.1:8800/api/config \
  -H 'Content-Type: application/json' \
  -d '{"bundle_path":"/Users/Shared/FarmDashboardBuilds/FarmDashboardController-0.1.9.184.dmg"}'

curl -fsS -X POST http://127.0.0.1:8800/api/upgrade
curl -fsS http://127.0.0.1:8800/api/status
```

Result: `current_version: 0.1.9.184` (previous `0.1.9.183`).

## Installed stack health smoke

```bash
make e2e-installed-health-smoke
```

Result: `PASS`.

## CI / Build

```bash
make ci-web-smoke
```

Result: `PASS` (lint warnings exist, no errors).

## Tier-A screenshots (Playwright)

```bash
cd apps/dashboard-web
node scripts/web-screenshots.mjs \
  --no-web \
  --no-core \
  --base-url=http://127.0.0.1:8000 \
  --api-base=http://127.0.0.1:8000 \
  --auth-token-file=/Users/Shared/FarmDashboardBuilds/playwright_screenshots_token_dw173_dw174.txt
```

Result: `PASS`.

Evidence (captured + viewed):
- `manual_screenshots_web/20260120_081428/analytics.png` (Water ticks readable at 24h; Weather forecast + PV charts render lines)
- `manual_screenshots_web/20260120_081428/nodes_reorder_modal.png` (Display order modal fits viewport; content scrollable)
- `manual_screenshots_web/20260120_081428/sensors_reorder_modal.png` (Same modal behavior from Sensors & Outputs)

